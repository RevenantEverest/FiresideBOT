import React from 'react';
import { PageNotFound } from '../../pages/PageNotFound';

function PageNotFoundContainer(props) {
    return(
        <PageNotFound {...props} />
    );
};

export default PageNotFoundContainer;