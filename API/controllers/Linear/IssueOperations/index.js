const handleCreate = require('./handleCreate');
const handleUpdate = require('./handleUpdate');
const handleRemove = require('./handleRemove');
const handleRestore = require('./handleRestore');

module.exports = {
    handleCreate,
    handleUpdate,
    handleRemove,
    handleRestore
};