import React from 'react';
import { CustomCommandsScreen } from '../../screens/CustomCommands';

function CustomCommandsContainer(props) {
    return(
        <CustomCommandsScreen {...props} />
    );
};

export default CustomCommandsContainer;