import React from 'react';
import { ModerationScreen } from '../../screens/Moderation';

function ModerationContainer(props) {
    return(
        <ModerationScreen {...props} />
    );
};

export default ModerationContainer;