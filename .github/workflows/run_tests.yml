name: test
on: 
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
      - uses: actions/checkout@master
        with:
          ref: ${{ github.ref }}
        
      - name: Run docker-compose script
        run: |
          ./scripts/start_dev.sh
