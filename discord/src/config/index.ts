import { Config } from '../types/config';

const config: Config = {
    userCount: 0,
    guildCount: 0,
    servers: [],
    version: "2.0.0",
    updatePending: false,
    commands: [],
    categories: []
};

export default config;